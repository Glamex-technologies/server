const Joi = require("joi");
const JoiHelper = require("../../helpers/joiHelper.helpers");

class ReferenceDataValidator {
  constructor() {
    // No validation needed for public GET endpoints
    // This file is included for consistency with the module structure
  }
}

module.exports = ReferenceDataValidator;
